
// const { v4: uuidv4 } = require('uuid');

// exports.generateReference = () => `txn_${uuidv4()}`;

// exports.formatAmount = (amount) => (amount / 100).toFixed(2); // Paystack uses kobo

// exports.sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));